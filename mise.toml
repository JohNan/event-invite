[tools]
node = "22"

[tasks.lint]
run = "npm run lint"
description = "Execute stylelint and eslint"

[tasks.format]
run = "npm run format"
description = "Format all source files with prettier"

[tasks.check]
run = "npm run lint && npm run format:check"
description = "Verify linting and code formatting"
